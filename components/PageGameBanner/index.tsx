import React from "react";

import PageGuild from "../PageGuild";

const GameBanner = () => {
    return (
        <div>
            <PageGuild />
        </div>
    );
};

export default GameBanner;
