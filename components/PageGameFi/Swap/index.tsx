import React from "react";

import styles from "./Swap.module.sass";

const Swap = () => {
    return (
        <div className={styles.row}>
            <div>Swap left</div>
            <div>right</div>
        </div>
    );
};

export default Swap;
